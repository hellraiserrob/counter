{
  "name": "counter",
  "version": "0.1.0",
  "engines": {
    "node": "6.9.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd server/ && npm install --only=dev && npm install cd ../frontend/ && npm install --only=dev && npm install && npm run build"
  },
  "cacheDirectories": [
    "frontend/node_modules",
    "server/node_modules"
  ],
  "dependencies": {},
  "devDependencies": {}
}